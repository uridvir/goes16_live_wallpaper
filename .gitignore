# .gitignore file. This file is a blacklist of files that I do NOT want git to pester me about adding.

# DownloadProject files (I haven't figured out how to auto-download these into the build directory)
DownloadProject.cmake
DownloadProject.CMakeLists.cmake.in

# Compilation database for clang-tidy
compile_commands.json

# Configuration for vim
init.vim

# Files generated by CMake
src/core/*.cmake
src/core/CMakeFiles/**

# The entirety of the build directory does not belong on a git repository
build/**
